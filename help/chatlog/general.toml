names = [
    ".chatlog",
    ".ailog",
    ".对话记录",
    ".历史记录"
]
description = """
查看 ```rpk .chat``` 的对话记录
"""

examples = [
    { input = ".chatlog" },
    { input = ".chatlog @群友" },
    { input = ".chatlog -t" },
    { input = ".chatlog -p 3" },
    { plain = "```rpk .对话记录 -调试 -页``` ```rpkov -2```" },
    { input = ".对话记录 -头" },
]

options = [
    { names = ["-p", "-page", "-页"], params = "页码", description = "只看特定轮（页）的对话", hint = "页码可正可负" },
    { names = ["-t", "-text", "-文本"], description = "查看单轮对话（文本形式）", hint = "默认为最后一轮对话" },
    { names = ["-d", "-debug", "-调试"], description = "查看单轮对话（包含思考和使用工具等调试信息）", hint = "默认为最后一轮对话" },
    { names = ["-first", "-head", "-头"], description = "只看第一轮对话，等同于 ```rpko -p 1```" },
    { names = ["-last", "-tail", "-尾"], description = "只看最后一轮对话，等同于 ```rpko -p``` ```rpkov -1```" },
]

misc = """
对话记录会在最后一次对话之后 2 小时内保留
"""
