names = [
    ".chatlog",
    ".对话记录",
    ".历史记录"
]
description = """
查看 ```rpk .chat``` 的对话记录
"""

examples = [
    { input = ".chatlog" },
    { input = ".chatlog @群友" },
    { input = ".chatlog -t" },
    { input = ".对话记录 -调试" },
    { input = ".对话记录 -尾" },
]

options = [
    { names = ["-t", "-text", "-文本"], description = "只看最后一轮对话（文本形式）" },
    { names = ["-d", "-debug", "-调试"], description = "只看最后一轮对话（包含思考和使用工具等细节）" },
    { names = ["-last", "-tail", "-尾"], description = "只看最后一轮对话" },
]

misc = """
对话记录会在最后一次对话之后 2 小时内保留
"""
